// Ambient module declarations for dynamic model imports used at runtime
// Prevent TypeScript from failing the build when files exist only on WIP branches
declare module "@/models/Tenant";
declare module "@/models/*";
